# Database Configuration
DATABASE_URL=postgresql://username:password@localhost/servicedesk

# File uploads handled by frontend Cloudinary integration

# JWT Configuration (Optional - currently disabled for deployment)
JWT_SECRET_KEY=your_jwt_secret_key

# Flask Configuration
FLASK_ENV=production
PORT=5000

# Email Configuration (Optional - for SendGrid integration)
SENDGRID_API_KEY=your_sendgrid_api_key
FROM_EMAIL=noreply@yourcompany.com